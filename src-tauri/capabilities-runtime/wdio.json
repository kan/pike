{
  "identifier": "wdio",
  "description": "WebDriver E2E automation permissions (issue #142). Registered at runtime in debug builds only via add_capability; never present in release.",
  "windows": [
    "main",
    "project-*",
    "global-*"
  ],
  "permissions": [
    "wdio-webdriver:default"
  ]
}
